# 526.md

本文档记录从写完 `525.md` 之后到当前为止，FedRAP / WANN / RDSI / RGFTD 的方法讨论、方向修正、代码实现、先验实验结论给出的启发、当前训练结果和作业状态。

当前本地文档路径：

```text
D:\Research_FL\526.md
```

当前主代码目录：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4
```

当前 SOTA 对比代码目录：

```text
/data/jianbingshen/yanghongji/fedlppaSOTA/code_v4
```

当前日期口径：

```text
本地当前日期: 2026-05-26 Asia/Shanghai
远端集群日志时间: 2026-05-25
```

---

## 1. 525.md 之后的核心背景

`525.md` 结束时，方法处在 `RDSI-BCD` 阶段：

```text
RDSI-BCD = Boundary/Core-Damage aware Region-wise Domain-Specialist Intervention
```

当时的核心判断是：

```text
WANN risk / RDSI active region 确实富集错误；
teacher pool 在局部区域有正收益信号；
但 RDSI 还没有被最终实验稳定证明为主要贡献。
```

之后我们继续观察和讨论，发现一个更根本的问题：

```text
继续让 teacher 直接进入 q / pseudo-label target，容易在两个极端之间摆动。
```

这两个极端是：

```text
1. 放宽 teacher release:
   teacher 进入 loss 明显，但容易引入前景噪声、HD95 恶化、Dice 大幅波动。

2. 收紧 teacher release:
   负迁移减少，但 RDSI 几乎没有有效 loss，最终贡献很弱。
```

因此，`525.md` 之后最重要的方法转向是：

```text
RDSI 不再让 teacher 直接改 q / pseudo-label。
RDSI 只负责 region-wise specialist selection。
跨 client teacher 知识通过 feature/prototype-level transfer 注入。
```

当前版本命名为：

```text
RDSI-Proto
```

完整名称：

```text
Region-wise Domain-Specialist Prototype Transfer
```

---

## 2. 为什么从 BVI / BCD 转向 RDSI-Proto

### 2.1 BVI / BCD 的主要问题

早期 RDSI-BVI / RDSI-BCD 仍然属于 teacher target intervention：

```text
RDSI selects teacher logits
RDSI produces active map and residual alpha
RGFTD constructs q or residual q
student is supervised by teacher-influenced q
```

即使使用 residual：

```text
q = student_prob + alpha * (teacher_target - student_prob)
```

teacher 仍然直接改变 segmentation target。这个设计带来的问题是：

```text
1. 如果 teacher 局部预测正确，可能短期提高 Dice。
2. 如果 teacher 局部预测有偏，尤其在 Polyp 上，会直接污染前景结构。
3. q-level intervention 对 HD95 很敏感。
4. 过释放和过收紧之间没有稳定平衡。
```

### 2.2 先验实验给出的关键启发

之前做过多轮先验实验，包括：

```text
1. Oracle teacher-region upper bound
2. Teacher specialization map
3. RDSI score vs oracle benefit correlation
4. WANN risk 是否覆盖错误区域
5. Teacher core damage / core conflict analysis
6. 当前 selection path 的 CPU / GPU diagnostics
```

这些实验的共同结论不是：

```text
teacher pool 完全没用
```

而是：

```text
teacher pool 有局部正收益信号，
但当前 q-level release 很难稳定把这个信号转化成最终 Dice / HD95。
```

具体表现为：

```text
1. WANN risk / RDSI active 区域确实富集错误。
2. 在部分 active region 上，oracle best teacher 优于 student。
3. Polyp 的 RDSI selection 信号比 Prostate 更清楚，但 coverage 和最终收益不足。
4. Prostate 上模型能超过 SOTA，但 RDSI active 很小，归因更多可能来自 WANN/base。
5. knowledge_gap 与 oracle benefit 弱相关或负相关，不能作为正向 multiplier。
6. teacher_core_damage 与 oracle benefit 存在稳定负相关，是更像安全信号的指标。
```

因此，新的判断是：

```text
teacher pool 的价值不应该继续通过 q-level pseudo target 强行释放。
更合理的是把 teacher 当作局部 feature/prototype knowledge source。
```

---

## 3. 当前方法版本：RDSI-Proto

### 3.1 方法一句话

当前方法定义为：

```text
WANN diagnoses weak-label risk.
RDSI selects region-wise cross-client domain specialists in WANN risk regions.
Selected teachers no longer overwrite q.
RGFTD transfers external knowledge through feature/prototype alignment.
```

更正式的表述：

```text
In sparse heterogeneous federated weak segmentation, RDSI-Proto treats cross-client teachers as region-local specialist feature providers. WANN first identifies uncertain or risky regions under weak supervision. RDSI then selects the most locally reliable domain specialist for each eligible region. Instead of altering pseudo-label targets, the selected teacher knowledge is transferred through action-aware decoder feature prototypes.
```

### 3.2 与最早 WANN + RGFTD 的区别

最早的 WANN + RGFTD 更接近：

```text
WANN: weak annotation noise-aware target construction
RGFTD: teacher logits / q-level distillation
```

当前 RDSI-Proto 是：

```text
WANN: weak-label risk diagnosis and core preservation
RDSI: region-wise teacher selection from cross-client teacher pool
RGFTD/Proto: feature prototype transfer rather than pseudo-label replacement
```

核心区别：

```text
1. teacher 不是 globally stronger model；
2. teacher 是 region-local specialist；
3. teacher 不再拥有直接修改 segmentation target 的权力；
4. cross-client knowledge 通过 decoder feature prototypes 传递；
5. knowledge_gap 不再正向驱动选择；
6. boundary / foreground repair / paired background 是 action-aware transfer 的组成部分。
```

---

## 4. 当前 RDSI-Proto 的完整流程

### 4.1 WANN

WANN 仍然是入口模块，职责不变：

```text
1. 从弱标注中区分 raw weak seed / WANN core / candidate / risk。
2. 生成 core_mask、soft_band、ignore_mask、reliability、candidate_mask、risk_mask。
3. 保护 weak seed 和 WANN hard core。
4. 给 RDSI 提供可干预区域。
```

WANN 不负责 teacher 选择，也不负责 feature transfer。

### 4.2 Teacher pool

当前仍保留 cross-client teacher pool：

```text
server / client state dicts
non-self client teachers
stable / audit teacher source
region-wise candidate teacher list
```

每个 teacher 都会对当前 batch 前向一次，得到：

```text
teacher_logits_list
teacher_feature_list
teacher_ids
```

其中：

```text
teacher_logits_list 用于 RDSI selection；
teacher_feature_list 用于 prototype transfer。
```

### 4.3 Region-wise RDSI selection

RDSI selection 仍然在 `select_rdsi_teacher_logits()` 中完成。

它会计算：

```text
teacher_local_reliability
student_local_risk
foreground lift
boundary support
core-preserving foreground support
core damage proxy
seed conflict
unsafe gap
foreground excess proxy
```

但是：

```text
knowledge_gap 不再作为正向乘子。
```

当前 positive signal 主要来自：

```text
foreground repair score
boundary score
teacher reliability
student risk
spatial support
```

negative / safety signal 主要来自：

```text
core_damage_proxy
seed_conflict
unsafe_gap
foreground_excess_proxy
```

### 4.4 当前 selection 的 action

当前已接入的 action 包括：

```text
1. foreground repair
2. boundary action
3. paired background
```

重要修正：

```text
之前 boundary_score 被计算，但 boundary_active_mask 被固定为 0。
这导致 boundary-aware RDSI 在 feature/prototype path 中只存在日志，不进入 loss。
```

当前已修正为：

```python
teacher_candidate_region = fg_repair_candidate | boundary_candidate
release_score = torch.maximum(fg_repair_score, boundary_score).clamp(0.0, 1.0)
```

并根据 score 决定：

```python
selected_primary_is_fg
selected_primary_is_boundary
fg_repair_active_mask
boundary_active_mask
```

最终 active map：

```python
active_mask = foreground_active_mask | background_active_mask | boundary_active_mask
```

### 4.5 Paired background

paired background 仍然保留，但它不是 standalone release。

当前原则：

```text
background 只能作为 foreground active 的局部配对背景；
不能自己独立成为 teacher release 主原因；
background pixels 数量受 foreground pixels 限制。
```

这继承了 RDSI-FG / RDSI-SC / RDSI-BCD 的核心经验：

```text
background-only release 容易造成 RDSI active 非零但 q/loss 无效，或者进一步压低 foreground。
```

在 RDSI-Proto 中，paired background 进入 feature prototype loss，而不是进入 q。

### 4.6 Feature/prototype transfer

新增核心函数：

```python
rdsi_feature_prototype_loss(student_feature, teacher_feature_list, args, iter_num)
```

调用位置：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4/flower_pCE_2D_v4_FedLPPA.py
```

核心逻辑：

```text
1. 使用 student decoder feature de1。
2. 使用 selected teacher 的对应 decoder feature。
3. 根据 RDSI selection 生成 teacher prototype weight stack。
4. 分 action 计算 feature prototype:
   - foreground repair prototype
   - background paired prototype
   - boundary prototype
5. 使用 cosine prototype alignment loss。
```

训练中对应：

```python
loss_rgftd, lambda_rgftd, rgftd_profile = rdsi_feature_prototype_loss(
    de1,
    teacher_feature_list,
    rgftd_teacher_args,
    self.current_iter,
)
loss_ce = loss_ce + float(lambda_rgftd) * loss_rgftd
```

这意味着：

```text
teacher 不再直接改变 q；
teacher 只通过 feature prototype 对 student decoder 表征施加约束。
```

---

## 5. 代码具体改动

### 5.1 `rgftd_reliability_distillation.py`

路径：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4/rgftd_reliability_distillation.py
D:\Research_FL\rgftd_reliability_distillation.py
```

主要改动：

```text
1. 新增 RDSI prototype profile keys。
2. 新增 runtime attrs:
   - _rdsi_teacher_proto_weight_stack
   - _rdsi_selected_teacher_stack_index
   - _rdsi_proto_region_weight
3. 移除 knowledge_gap 的正向 multiplier。
4. select_rdsi_teacher_logits() 只负责 selection 和 runtime map。
5. 新增 rdsi_feature_prototype_loss()。
6. 修正 boundary action 被计算但不 active 的问题。
7. 修正 no-active batch 的 lambda 日志口径。
```

新增/使用的重要日志：

```text
rdsi_proto_loss
rdsi_proto_weight_mean
rdsi_proto_weight_top
rdsi_proto_cosine
rdsi_proto_region_ratio
rdsi_proto_teacher_entropy
rdsi_proto_teacher_weight_max
rdsi_proto_valid_batches
rdsi_fg_repair_loss
rdsi_bg_suppress_loss
rdsi_boundary_loss
```

### 5.2 `flower_pCE_2D_v4_FedLPPA.py`

路径：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4/flower_pCE_2D_v4_FedLPPA.py
D:\Research_FL\flower_pCE_2D_v4_FedLPPA.py
```

主要改动：

```text
1. import rdsi_feature_prototype_loss。
2. 新增 _rdsi_feature(model_out)，抽取 model output index 2 的 de1 feature。
3. teacher pool forward 时同时收集:
   - teacher_logits_list
   - teacher_feature_list
4. RDSI teacher pool path 中:
   - select_rdsi_teacher_logits() 只用于建立 selection map
   - rdsi_feature_prototype_loss() 计算实际 teacher transfer loss
5. 日志中加入 proto loss / proto region / proto cosine / boundary loss。
6. RDSI-enabled teacher pool path 不再 fallback 到旧 single-teacher q-level distillation。
```

### 5.3 旧 `rgftd_loss` 是否还存在

旧函数仍然存在：

```python
rgftd_loss(...)
```

原因：

```text
1. stable/audit 仍会调用；
2. legacy / non-RDSI path 仍可能使用；
3. 保留兼容性。
```

但当前 RDSI-Proto 主路径是：

```text
teacher pool -> select_rdsi_teacher_logits -> rdsi_feature_prototype_loss
```

因此：

```text
旧 q-level RGFTD loss 不再是当前 RDSI-Proto 的主训练路径。
```

---

## 6. Review 中发现并修正的问题

### 6.1 boundary 分支只算分、不进入 loss

发现：

```text
boundary_score 有计算；
rdsi_boundary_score 有日志；
rdsi_boundary_loss 有 profile key；
但 boundary_active_mask 被固定为 zero。
```

这会导致：

```text
Boundary/Core-Damage aware 的思想没有完整进入 feature/prototype transfer。
```

修正：

```python
teacher_candidate_region = fg_repair_candidate | boundary_candidate
release_score = torch.maximum(fg_repair_score, boundary_score).clamp(0.0, 1.0)

selected_primary_is_fg = ...
selected_primary_is_boundary = ...

fg_repair_active_mask = selected_active_mask & selected_primary_is_fg
boundary_active_mask = selected_active_mask & selected_primary_is_boundary

active_mask = foreground_active_mask | background_active_mask | boundary_active_mask
```

### 6.2 no-active batch 的 lambda 日志误导

发现：

```text
当 RDSI 没有选中任何区域时，loss 正确为 0；
但 lambda_pre_safety / lambda_effective 没有被正确填入 profile。
```

这会导致日志看起来像：

```text
RDSI/RGFTD 没启动
```

实际上应该解释为：

```text
RDSI 已启用，但当前 batch 没有 selected region。
```

修正：

```python
lambda_raw = float(get_rgftd_lambda(iter_num, args))
lambda_cap = float(getattr(args, "rgftd_lambda_eff_cap", lambda_raw))
...
profile["lambda"] = torch.tensor(lambda_raw, device=device)
profile["lambda_pre_safety"] = torch.tensor(lambda_raw, device=device)
profile["lambda_after_safety"] = torch.tensor(lambda_effective, device=device)
profile["lambda_effective"] = torch.tensor(lambda_effective, device=device)
```

即使 no active region：

```text
loss = 0
lambda log = scheduled lambda / capped lambda
```

这让后续日志解释更清楚。

---

## 7. 代码验证

### 7.1 本地验证

本地 Windows 目录：

```text
D:\Research_FL
```

执行过：

```text
python -m py_compile D:\Research_FL\rgftd_reliability_distillation.py D:\Research_FL\flower_pCE_2D_v4_FedLPPA.py
```

结果：

```text
通过
```

本地没有 torch，因此本地不能跑张量 smoke test。

### 7.2 远端同步

已同步到：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4/
```

文件：

```text
rgftd_reliability_distillation.py
flower_pCE_2D_v4_FedLPPA.py
```

### 7.3 远端编译

执行：

```text
cd /data/jianbingshen/yanghongji/FedLPPA_Original/code_v4
python3 -m py_compile rgftd_reliability_distillation.py flower_pCE_2D_v4_FedLPPA.py
```

结果：

```text
通过
```

### 7.4 远端 conda 环境 smoke test

训练脚本使用环境：

```text
conda activate fed39v2
```

在 `fed39v2` 中跑了最小 feature-prototype smoke test。

结果：

```text
loss 0.832128
lambda 0.02
lambda_pre 0.10000000149011612
proto_region 0.125
boundary_loss 0.943849
grad_ok True

zero_loss -0.0
zero_lambda_return 0.0
zero_lambda_pre_log 0.10000000149011612
zero_lambda_eff_log 0.019999999552965164
```

解释：

```text
1. feature-prototype loss 能产生非零 loss。
2. boundary loss 能正常计算。
3. loss 对 student feature 有梯度。
4. no-active region 时 loss 为 0，但 lambda 日志仍保留真实 schedule。
```

---

## 8. 当前数据协议

当前继续使用 `rdsi3_sd`：

```text
scribble-dominant heterogeneous weak-label protocol
```

数据路径：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/data/PROSTATE_h5_rdsi3_sd
/data/jianbingshen/yanghongji/FedLPPA_Original/data/POLYP_h5_rdsi3_sd
/data/jianbingshen/yanghongji/FedLPPA_Original/data/FAZ_h5_rdsi3_sd
```

client weak-label type：

```text
Prostate, 6 clients:
client1: scribble
client2: keypoint
client3: scribble
client4: block
client5: scribble
client6: scribble

Polyp, 4 clients:
client1: scribble
client2: keypoint
client3: scribble
client4: block

FAZ, 5 clients:
client1: scribble
client2: keypoint
client3: scribble
client4: block
client5: scribble
```

这个协议的目的：

```text
1. 不再依赖 FedLPPA 原始 box-heavy 设定。
2. 保留 heterogeneous weak supervision。
3. 让 scribble 成为主弱标注类型。
4. 保留 keypoint / block，避免退化成 uniform scribble-only。
```

---

## 9. 525.md 之后的训练观察

### 9.1 BVIAlign 作业

在 RDSI-Proto 之前，当前仍有 BVIAlign 作业在跑：

```text
120599 BVIAlign_PROS RUNNING
120600 BVIAlign_POLY RUNNING
120601 BVIAlign_FAZ  PENDING
```

这些作业是旧版本，不是 RDSI-Proto。

### 9.2 BVIAlign Prostate 当前结果

日志：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4/logs/run_bvialign_prostate_r500_l10_20260525_142307_seed2022/server.log
```

截至当前查询：

```text
eval_count = 219
last iter = 2190
last Dice = 0.704807
last HD95 = 22.520047
last Recall = 0.758363
last Precision = 0.744442
last JC = 0.592565
last Specificity = 0.950576
last RAVD = 0.253973
```

当前 best：

```text
best iter = 2010
best Dice = 0.762549
best HD95 = 17.522224
best Recall = 0.798166
best Precision = 0.784625
best JC = 0.653027
best Specificity = 0.978568
best RAVD = 0.218078
```

判断：

```text
Prostate 在旧 BVIAlign 下有一次很高的冲高点。
但之后回落到 0.68-0.70 左右，仍然波动较大。
```

### 9.3 BVIAlign Polyp 当前结果

日志：

```text
/data/jianbingshen/yanghongji/FedLPPA_Original/code_v4/logs/run_bvialign_polyp_r500_l10_20260525_142307_seed2022/server.log
```

截至当前查询：

```text
eval_count = 281
last iter = 2810
last Dice = 0.524209
last HD95 = 103.627511
last Recall = 0.769487
last Precision = 0.477863
last JC = 0.403754
last Specificity = 0.902772
last RAVD = 3.926939
```

当前 best：

```text
best iter = 2410
best Dice = 0.548125
best HD95 = 89.073160
best Recall = 0.721261
best Precision = 0.530780
best JC = 0.432128
best Specificity = 0.925932
best RAVD = 3.976431
```

判断：

```text
Polyp 仍然明显不理想。
HD95 很高，RAVD 很大，说明存在明显脏前景/过分割问题。
这也进一步支持不要继续通过 q-level teacher target 强推前景。
```

---

## 10. RDSI-Proto 作业提交情况

用户要求：

```text
启动新版本 prostate / polyp / faz；
不要暂停当前正在跑的作业；
让新作业自然等待资源。
```

已提交：

```text
120602 RDSIProto_PROS
120604 RDSIProto_POLY
120603 RDSIProto_FAZ
```

当前状态：

```text
120602 RDSIProto_PROS PENDING QOSMaxJobsPerUserLimit
120604 RDSIProto_POLY PENDING QOSMaxJobsPerUserLimit
120603 RDSIProto_FAZ  PENDING QOSMaxJobsPerUserLimit
```

独立 run prefix：

```text
rdsiproto_prostate_r500_l10
rdsiproto_polyp_r500_l10
rdsiproto_faz_r500_l10
```

独立端口：

```text
Prostate: 127.0.0.1:8642
Polyp:    127.0.0.1:8644
FAZ:      127.0.0.1:8645
```

提交命令等价于：

```text
sbatch --job-name=RDSIProto_PROS --export=ALL,RUN_PREFIX=rdsiproto_prostate_r500_l10,SERVER_ADDRESS=127.0.0.1:8642 run_prostate_rdsi_bcd_rgftd_r500_l10.sh

sbatch --job-name=RDSIProto_POLY --export=ALL,RUN_PREFIX=rdsiproto_polyp_r500_l10,SERVER_ADDRESS=127.0.0.1:8644 run_polyp_rdsi_bcd_rgftd_r500_l10.sh

sbatch --job-name=RDSIProto_FAZ --export=ALL,RUN_PREFIX=rdsiproto_faz_r500_l10,SERVER_ADDRESS=127.0.0.1:8645 run_faz_rdsi_bcd_rgftd_r500_l10.sh
```

注意：

```text
脚本文件名仍是 rdsi_bcd_rgftd；
但代码内容已经同步为 RDSI-Proto；
作业名和 run prefix 明确区分为 RDSIProto。
```

---

## 11. SOTA / 历史结果上下文

根据 `525.md` 中记录：

```text
RDSI-BCD Prostate best Dice: 0.7442334676
SOTA Prostate best Dice:    约 0.672425
```

但是：

```text
Prostate 的 RDSI active 很小；
因此 Prostate 超 SOTA 不应直接归因给 RDSI。
```

Polyp：

```text
RDSI-BCD Polyp best Dice: 0.6194895114
SOTA Polyp best Dice:    约 0.675652
```

因此：

```text
RDSI-BCD 在 Prostate 上数值好，但归因弱；
RDSI-BCD 在 Polyp 上有局部 selection 信号，但最终没有超过 SOTA；
BVIAlign Polyp 目前更低，说明 q-level target intervention 仍然不稳定。
```

这直接推动了 RDSI-Proto：

```text
保留 cross-client teacher pool 的核心创新，
但从 q-level target intervention 改成 feature/prototype-level transfer。
```

---

## 12. 目前如何解释日志

RDSI-Proto 之后，不能再用旧标准解释所有字段。

### 12.1 仍然重要的字段

应重点看：

```text
rgftd_rdsi_proto_loss
rgftd_rdsi_proto_weight_mean
rgftd_rdsi_proto_weight_top
rgftd_rdsi_proto_cosine
rgftd_rdsi_proto_region_ratio
rgftd_rdsi_proto_teacher_entropy
rgftd_rdsi_proto_teacher_weight_max
rgftd_rdsi_proto_valid_batches
rgftd_rdsi_fg_repair_loss
rgftd_rdsi_bg_suppress_loss
rgftd_rdsi_boundary_loss
rgftd_rdsi_boundary_active_ratio
rgftd_rdsi_fg_repair_active_ratio
rgftd_rdsi_background_paired_ratio
```

### 12.2 旧 q-delta 字段的解释

旧字段仍可能存在：

```text
rdsi_q_fg_delta
rdsi_fg_repair_q_delta
rdsi_bg_suppress_q_delta
rdsi_boundary_q_delta
```

但在 RDSI-Proto 中：

```text
teacher 不再直接修改 q。
```

因此这些字段长期为 0 不再必然表示 RDSI 无效。

当前更准确的判断依据是：

```text
proto loss 是否非零；
proto region 是否非零；
boundary / fg / bg action loss 是否进入；
teacher selection ratio 是否合理；
最终 Dice / HD95 是否改善。
```

### 12.3 no-active region 的解释

修正后：

```text
lambda_pre_safety / lambda_effective 可以非零；
loss 可以为 0；
proto_region 可以为 0。
```

这表示：

```text
schedule 已进入 RDSI 阶段；
但该 batch 没有被 RDSI 选中的有效 region。
```

不能再简单把 `loss=0` 解读为代码没启动。

---

## 13. 当前最重要的实验问题

### 13.1 RDSI-Proto 是否能比 BVI / BCD 更稳定

RDSI-Proto 的目标不是简单扩大 active region，而是：

```text
减少 q-level negative transfer；
保留 cross-client teacher feature knowledge；
让 teacher 的影响变成表征级约束。
```

需要观察：

```text
Prostate:
是否还能保持或超过 BVIAlign / BCD 的高点；
是否比 q-level intervention 更稳定。

Polyp:
HD95 是否下降；
RAVD 是否收敛；
Dice 是否能超过 BVIAlign / BCD，并接近或超过 SOTA。

FAZ:
RDSI-Proto 是否能正常启动；
proto loss 是否有效进入；
是否比旧 BCD 更稳。
```

### 13.2 是否仍需要 WANN-only ablation

仍然需要。

原因：

```text
Prostate 上多次出现 method 超 SOTA 但 RDSI active 很弱。
如果 WANN-only 已经接近 RDSI，那么 RDSI 的论文归因仍然不稳。
```

后续应补：

```text
same protocol + same data + WANN enabled + RDSI/RGFTD disabled
```

用于判断：

```text
FedRAP 的收益来自 WANN，还是来自 RDSI-Proto。
```

### 13.3 是否还需要 oracle diagnostics

仍然有价值，但方向要变：

旧 oracle 主要验证：

```text
teacher logits / q-level intervention 是否比 student 更接近 GT。
```

RDSI-Proto 后应增加：

```text
selected teacher feature prototype 是否比 random / non-selected teacher 更接近 oracle-positive region；
selected teacher feature 是否在 boundary / foreground repair region 上更有判别性；
proto alignment 后是否减少 false foreground / boundary outlier。
```

不过短期最重要的是先跑 RDSI-Proto 的真实训练曲线。

---

## 14. 当前风险和注意事项

### 14.1 代码层面

当前已通过：

```text
py_compile
远端 conda smoke test
feature prototype gradient test
```

目前没有发现：

```text
语法错误
张量维度错误
明显违背 RDSI-Proto idea 的主路径错误
```

但仍需真实训练验证：

```text
teacher_feature_list 是否在大 batch / 多 client 下稳定；
proto_weight_stack 是否与 action mask 长期匹配；
boundary action 是否会过多抢占 foreground repair；
proto loss 是否过弱或过强。
```

### 14.2 作业层面

当前有：

```text
2 个 RUNNING:
120599 BVIAlign_PROS
120600 BVIAlign_POLY

4 个 PENDING:
120601 BVIAlign_FAZ
120602 RDSIProto_PROS
120603 RDSIProto_FAZ
120604 RDSIProto_POLY
```

由于 `QOSMaxJobsPerUserLimit`：

```text
RDSIProto 会自然等待资源；
当前没有暂停 BVIAlign。
```

如果后续要优先跑 RDSIProto：

```text
需要明确是否停掉 BVIAlign 或旧 BVIAlign_FAZ。
```

目前用户明确要求：

```text
不要暂停现在在跑的作业。
```

所以当前只提交等待队列。

### 14.3 日志/模型清理

仍然遵守：

```text
不要删除后续评测可能用到的 log。
可以删除副本、tensorboard、重复 pth，但必须明确对应作业和用途。
```

当前 RDSIProto 还未开始，不应删除其未来日志目录。

---

## 15. 当前总结

从 `525.md` 到现在，方法的最重要转向是：

```text
从 RDSI-BCD 的 q-level residual teacher intervention
转向 RDSI-Proto 的 feature/prototype-level cross-client specialist transfer。
```

为什么这样转：

```text
1. 多轮先验实验显示 teacher pool 有局部正收益，但 q-level release 难以稳定。
2. Polyp 上 q-level teacher intervention 容易造成 HD95 / RAVD 恶化。
3. Prostate 上高 Dice 不足以证明 RDSI 归因，因为 RDSI active 往往很弱。
4. knowledge_gap 不能作为正向 release 依据。
5. core damage / boundary support / foreground repair 更符合先验实验信号。
```

当前 RDSI-Proto 保留的核心创新：

```text
cross-client external knowledge
region-wise domain specialist selection
WANN-guided risk region
teacher pool
boundary/core-damage aware selection
action-aware transfer
```

当前 RDSI-Proto 放弃或弱化的部分：

```text
teacher direct q replacement
teacher-driven pseudo-label target
knowledge_gap positive multiplier
global teacher superiority assumption
```

当前已经完成：

```text
1. 代码改成 feature/prototype transfer。
2. 修正 boundary active 逻辑。
3. 修正 lambda 日志口径。
4. 本地和远端编译通过。
5. 远端 fed39v2 smoke test 通过。
6. 提交 RDSIProto_PROS / POLY / FAZ 等待资源。
```

当前仍需等待：

```text
RDSIProto_PROS / POLY / FAZ 真正开始运行；
观察 800 iter 后 proto loss 是否进入；
观察 1500-2500 iter 的 Dice / HD95 趋势；
与 SOTA、BVIAlign、RDSI-BCD 做同阶段和 best 对比。
```

当前最准确的研究定位：

```text
RDSI-Proto 是对前面 RDSI 系列的一次结构性修正。
它不是继续调 gate，也不是删除 teacher。
它把 teacher 的作用从 pseudo-label authority 改成 feature-level specialist knowledge source。
```
